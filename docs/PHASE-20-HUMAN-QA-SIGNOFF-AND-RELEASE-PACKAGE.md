# المرحلة 20: اعتماد الاختبار اليدوي وتجهيز حزمة التسليم

## الغرض من المرحلة

تهدف هذه المرحلة إلى تجهيز المشروع لمراجعة بشرية نهائية قبل التسليم، عن طريق توثيق اختبار يدوي واضح، وتجهيز حزمة ويندوز محمولة، وتحديد ما يجب على المختبر البشري تأكيده قبل تسليم التطبيق لصاحب العمل.

هذه المرحلة ليست مرحلة تطوير وظائف جديدة، ولم يتم فيها إضافة وحدات أعمال أو تغيير منطق التشغيل.

## خط الأساس الحالي للإصدار

- آخر التزام: `eaa4a28 Phase 19 Windows build smoke test readiness`.
- الوسم الحالي: `phase-19-windows-build-smoke-test`.
- نتيجة المرحلة 19: بناء ويندوز ناجح، وتشغيل الملف التنفيذي ناجح، مع قرار `CONDITIONAL PASS` لأن الاختبار اليدوي الكامل للواجهة لم يكتمل بشريا.
- نطاق النظام: مخزن حبوب واحد، دوران فقط هما المالك والموظف، وواجهة عربية.

## حالة البناء

- `flutter.bat test`: نجح، 227 اختبارا.
- `flutter.bat analyze --no-pub`: نجح، لا توجد مشاكل.
- `flutter.bat build windows`: نجح.
- `git diff --check`: نجح بدون أخطاء مسافات، مع ملاحظات نهاية أسطر فقط في ملفات Flutter مولدة.

ظهرت أثناء البناء تحذيرات غير مانعة من Firebase CMake و`LNK4078`. لا تمنع هذه التحذيرات إنتاج نسخة ويندوز.

## مسار ملف التشغيل

افتح الملف التالي للتجربة من مخرجات البناء:

`C:\dev\multi-pos\grain-warehouse-erp-lite\build\windows\x64\runner\Release\grain_warehouse_erp_lite.exe`

## مسار الحزمة المحمولة

تم تجهيز حزمة محمولة في:

`C:\dev\multi-pos\grain-warehouse-erp-lite\release\grain-warehouse-erp-lite-phase-20-portable\`

ملف التشغيل داخل الحزمة:

`release\grain-warehouse-erp-lite-phase-20-portable\app\grain_warehouse_erp_lite.exe`

ملف الإرشادات داخل الحزمة:

`release\grain-warehouse-erp-lite-phase-20-portable\README-AR.txt`

أرشيف الحزمة:

`C:\dev\multi-pos\grain-warehouse-erp-lite\release\grain-warehouse-erp-lite-phase-20-portable.zip`

## نتيجة تشغيل الحزمة المحمولة

تم تشغيل الملف التنفيذي من داخل الحزمة المحمولة:

`C:\dev\multi-pos\grain-warehouse-erp-lite\release\grain-warehouse-erp-lite-phase-20-portable\app\grain_warehouse_erp_lite.exe`

- رقم العملية أثناء التحقق: `12188`.
- عنوان النافذة: `grain_warehouse_erp_lite`.
- حالة الاستجابة: `Responding: True`.
- لم يظهر عطل فوري أثناء نافذة التحقق.
- تم إيقاف العملية بعد التحقق حتى لا تبقى نسخة مفتوحة.

## جدول اعتماد الاختبار اليدوي

| المجال | الحالة | اسم المختبر | التاريخ | ملاحظات |
|---|---|---|---|---|
| المالك | يتطلب تأكيد يدوي من المستخدم |  |  |  |
| الموظف | يتطلب تأكيد يدوي من المستخدم |  |  |  |
| النسخ الاحتياطي | يتطلب تأكيد يدوي من المستخدم |  |  |  |
| الاستعادة | يتطلب تأكيد يدوي من المستخدم |  |  |  |
| مسح البيانات | يتطلب تأكيد يدوي من المستخدم |  |  |  |
| العربية والتخطيط | يتطلب تأكيد يدوي من المستخدم |  |  |  |
| حزمة التسليم | يتطلب تأكيد يدوي من المستخدم |  |  |  |

## قائمة اختبار المالك

- تشغيل التطبيق من مجلد `Release`: يتطلب تأكيد يدوي من المستخدم.
- تشغيل التطبيق من الحزمة المحمولة: يتطلب تأكيد يدوي من المستخدم.
- عمل مسار أول تشغيل إذا لم توجد بيانات محلية: يتطلب تأكيد يدوي من المستخدم.
- نجاح تسجيل دخول المالك: يتطلب تأكيد يدوي من المستخدم.
- ظهور التنقل الرئيسي الصحيح للمالك: يتطلب تأكيد يدوي من المستخدم.
- إنشاء صنف جديد: يتطلب تأكيد يدوي من المستخدم.
- إنشاء مورد جديد: يتطلب تأكيد يدوي من المستخدم.
- تنفيذ شراء أو إدخال مخزون: يتطلب تأكيد يدوي من المستخدم.
- زيادة كمية المخزون بعد الشراء أو الإدخال: يتطلب تأكيد يدوي من المستخدم.
- تنفيذ بيع: يتطلب تأكيد يدوي من المستخدم.
- نقص كمية المخزون بعد البيع: يتطلب تأكيد يدوي من المستخدم.
- عمل إلغاء أو عكس البيع إذا كان متاحا في الواجهة: يتطلب تأكيد يدوي من المستخدم.
- فتح تقرير اليوم: يتطلب تأكيد يدوي من المستخدم.
- فتح سجل المستندات أو شاشة التدقيق إذا كانت متاحة: يتطلب تأكيد يدوي من المستخدم.
- فتح شاشة تصدير النسخة الاحتياطية: يتطلب تأكيد يدوي من المستخدم.
- حفظ ملف نسخة احتياطية حقيقي: يتطلب تأكيد يدوي من المستخدم.
- العثور على ملف النسخة الاحتياطية على القرص: يتطلب تأكيد يدوي من المستخدم.
- ظهور ملف النسخة الاحتياطية كملف JSON صالح ظاهريا: يتطلب تأكيد يدوي من المستخدم.
- فتح معاينة الاستعادة: يتطلب تأكيد يدوي من المستخدم.
- قبول المعاينة لنسخة احتياطية صالحة: يتطلب تأكيد يدوي من المستخدم.
- رفض المعاينة لنسخة احتياطية غير صالحة: يتطلب تأكيد يدوي من المستخدم.
- منع الاستعادة فوق بيانات موجودة: يتطلب تأكيد يدوي من المستخدم.
- فتح شاشة مسح البيانات للمالك: يتطلب تأكيد يدوي من المستخدم.
- طلب عبارة تأكيد مكتوبة بدقة قبل المسح: يتطلب تأكيد يدوي من المستخدم.
- فرض النسخة الاحتياطية قبل الحذف: يتطلب تأكيد يدوي من المستخدم.
- عدم حذف البيانات عند كتابة عبارة مسح خاطئة: يتطلب تأكيد يدوي من المستخدم.
- بعد المسح الناجح تكون البيانات التشغيلية فارغة: يتطلب تأكيد يدوي من المستخدم.
- بقاء بيانات المالك والمصادقة والجلسة: يتطلب تأكيد يدوي من المستخدم.
- بقاء التطبيق قابلا للاستخدام بعد المسح: يتطلب تأكيد يدوي من المستخدم.

## قائمة اختبار الموظف

- نجاح تسجيل دخول الموظف: يتطلب تأكيد يدوي من المستخدم.
- قدرة الموظف على استخدام الشاشات اليومية المسموحة: يتطلب تأكيد يدوي من المستخدم.
- منع الموظف من الوصول إلى مسح البيانات: يتطلب تأكيد يدوي من المستخدم.
- منع الموظف من الإجراءات الخطرة الخاصة بالمالك: يتطلب تأكيد يدوي من المستخدم.
- منع الموظف من الاستعادة أو المسح: يتطلب تأكيد يدوي من المستخدم.
- عدم ظهور خيارات تدميرية خاصة بالمالك في تنقل الموظف: يتطلب تأكيد يدوي من المستخدم.
- عدم قدرة الموظف على تجاوز القيود من الواجهة: يتطلب تأكيد يدوي من المستخدم.

## قائمة اختبار النسخ الاحتياطي

- إنشاء ملف نسخة احتياطية قبل المسح: يتطلب تأكيد يدوي من المستخدم.
- بقاء البيانات عند إلغاء أو فشل تصدير النسخة الاحتياطية: يتطلب تأكيد يدوي من المستخدم.
- حفظ الملف في مكان واضح على القرص: يتطلب تأكيد يدوي من المستخدم.
- فتح الملف كـ JSON قابل للقراءة: يتطلب تأكيد يدوي من المستخدم.
- عدم احتواء النسخة على بيانات جلسة أو مصادقة حساسة: يتطلب تأكيد يدوي من المستخدم.

## قائمة اختبار الاستعادة

- عمل الاستعادة فقط داخل نظام فارغ: يتطلب تأكيد يدوي من المستخدم.
- استمرار منع الاستعادة فوق بيانات موجودة: يتطلب تأكيد يدوي من المستخدم.
- عدم وجود دمج استيراد: يتطلب تأكيد يدوي من المستخدم.
- عدم وجود استيراد جزئي: يتطلب تأكيد يدوي من المستخدم.
- رفض النسخة غير الصالحة برسالة واضحة: يتطلب تأكيد يدوي من المستخدم.
- عدم تعديل بيانات المصادقة أو الجلسة أثناء الاستعادة: يتطلب تأكيد يدوي من المستخدم.

## قائمة اختبار مسح البيانات

- عدم تنفيذ المسح دون نسخة احتياطية محفوظة: يتطلب تأكيد يدوي من المستخدم.
- عدم تنفيذ المسح عند فشل حفظ النسخة الاحتياطية: يتطلب تأكيد يدوي من المستخدم.
- عدم تنفيذ المسح مع عبارة تأكيد خاطئة: يتطلب تأكيد يدوي من المستخدم.
- عدم إنشاء سجلات إلغاء أثناء المسح: يتطلب تأكيد يدوي من المستخدم.
- عدم إنشاء حركات مخزون عكسية أثناء المسح: يتطلب تأكيد يدوي من المستخدم.
- عدم حذف بيانات المالك أو المصادقة أو الجلسة: يتطلب تأكيد يدوي من المستخدم.
- وضوح رسالة الخطر قبل التنفيذ: يتطلب تأكيد يدوي من المستخدم.

## قائمة اختبار العربية والتخطيط

- النص العربي مقروء: يتطلب تأكيد يدوي من المستخدم.
- اتجاه الواجهة من اليمين إلى اليسار مقبول: يتطلب تأكيد يدوي من المستخدم.
- عدم وجود حروف عربية مكسورة أو مشوهة: يتطلب تأكيد يدوي من المستخدم.
- وضوح الأزرار: يتطلب تأكيد يدوي من المستخدم.
- وضوح رسائل التحذير: يتطلب تأكيد يدوي من المستخدم.
- ظهور الشاشات الخطرة بشكل جاد ومفهوم: يتطلب تأكيد يدوي من المستخدم.
- قابلية استخدام تخطيط سطح المكتب: يتطلب تأكيد يدوي من المستخدم.
- قبول التخطيط في نافذة أصغر أو توثيق القيد: يتطلب تأكيد يدوي من المستخدم.
- عدم وجود نص إنجليزي مربك في الشاشات الحرجة إلا عند الضرورة: يتطلب تأكيد يدوي من المستخدم.

## قائمة أمان البيانات

- لا تستخدم بيانات عميل حقيقية في أول جولة اختبار.
- استخدم بيانات مؤقتة واضحة مثل أصناف وموردين وفواتير تجربة.
- تأكد أن النسخة الاحتياطية تحفظ قبل أي تجربة مسح.
- لا تجرب الاستعادة أو المسح على بيانات حقيقية دون نسخة احتياطية مؤكدة.
- لا تحذف ملفات مجلد التطبيق داخل الحزمة المحمولة.
- لا تعدل ملفات JSON يدويا ثم تعتمد النتيجة كاختبار إنتاج.

## ما يجب اختباره ببيانات مؤقتة فقط

- أول تشغيل وإنشاء المالك.
- إنشاء صنف ومورد.
- إدخال شراء أو مخزون.
- تنفيذ بيع.
- إلغاء بيع إذا كان متاحا.
- تصدير نسخة احتياطية.
- معاينة نسخة احتياطية غير صالحة.
- مسح البيانات.
- الاستعادة إلى نظام فارغ.

## ما لا يجب اختباره على بيانات حقيقية دون نسخة احتياطية

- مسح البيانات.
- الاستعادة.
- اختبار فشل النسخة الاحتياطية.
- تعديل ملفات النسخة الاحتياطية يدويا.
- تشغيل نفس البيانات على نسخ متعددة من التطبيق دون خطة واضحة.

## قائمة محتويات حزمة التسليم

- مجلد `app`: موجود، ويحتوي ملفات تشغيل ويندوز.
- ملف `app\grain_warehouse_erp_lite.exe`: موجود.
- ملف `app\flutter_windows.dll`: موجود.
- مجلد `app\data`: موجود.
- ملف `app\firebase_core_plugin.lib`: موجود.
- ملف `README-AR.txt`: موجود داخل مجلد الحزمة.
- ملف أرشيف مضغوط للحزمة: تم إنشاؤه في `release\grain-warehouse-erp-lite-phase-20-portable.zip`.

## تعليمات التسليم

- سلم مجلد `grain-warehouse-erp-lite-phase-20-portable` كاملا أو ملف الضغط الناتج عنه.
- لا تسلم ملف التشغيل وحده، لأن التطبيق يحتاج الملفات الموجودة بجانبه.
- اطلب من المختبر تشغيل `app\grain_warehouse_erp_lite.exe`.
- اطلب من المختبر تسجيل نتائج الجدول في هذا المستند أو في نسخة منفصلة منه.
- عند ظهور مشكلة، يجب تسجيل لقطة شاشة، والخطوات الدقيقة، والدور المستخدم، وهل البيانات تجريبية أم حقيقية.

## القيود المعروفة

- النسخة المحمولة ليست مثبتا موقعا.
- قد يظهر تحذير Windows SmartScreen بسبب عدم توقيع النسخة.
- لا يوجد مثبت NSIS أو Inno Setup أو MSIX في هذه المرحلة.
- الاختبار اليدوي الكامل لم يكتمل بواسطة مستخدم بشري بعد.
- تحذيرات البناء غير المانعة من Firebase CMake و`LNK4078` ما زالت موجودة.
- مجلد `release/` ناتج تسليم مولد، وليس مضافا تلقائيا إلى Git.

## عوائق الإصدار

لا يوجد عائق بناء مؤكد في هذه المرحلة. العائق الوحيد قبل اعتماد تسليم نهائي هو إكمال الاختبار اليدوي البشري وتسجيل النتائج.

## القرار النهائي

`CONDITIONAL PASS`

السبب: البناء والاختبارات والتحليل وحزمة ويندوز المحمولة جاهزة، لكن اعتماد واجهة المالك والموظف والنسخ الاحتياطي والاستعادة والمسح والعربية يحتاج تأكيد يدوي من المستخدم قبل إعلان `PASS`.
